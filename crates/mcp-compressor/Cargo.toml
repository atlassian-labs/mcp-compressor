[package]
name    = "mcp-compressor"
version = "0.1.0"
edition = "2021"

[dependencies]
mcp-compressor-core = { path = "../mcp-compressor-core" }

[dev-dependencies]
serde_json = "1"
tokio      = { version = "1", features = ["macros"] }
